function PrintReversedString(firstChar, secondChar, thirdChar)
{
    let chars=[];
    chars.push(thirdChar);
    chars.push(secondChar);
    chars.push(firstChar);

    
    console.log(chars.join(' '));
}